{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default development capability set for CyberFiles.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default", "window-state:default"]
}
